using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;
public class PoliceSystem : MonoBehaviour
{
    /*
    public GameObject[] Stars;
    [Range(-1f, 10f)]
    public float yildizseviyesi ;
    void Start()
    {
        
    }

    // Update is called once per frame
    void FixedUpdate()
    {
        //Polis spawn süresi
        if (yildizseviyesi > 0)
        {
            float PoliceSpawnTime = ((10 - yildizseviyesi) * 2) + 15;
            if (yildizseviyesi > 4) // Bir Yerden sonra olaya Swat ekipleride katılıyor
            {
                float SwatSpawnTime = ((10 - yildizseviyesi) * 2) + 10;
            }
        }
        for(int i = 0 ; i <7; i++)
        {
            if (i < yildizseviyesi)
            {
                Stars[i].GetComponent<RawImage>().color = new Color(0, 0, 0 );
            }
            else
            {
                Stars[i].GetComponent<RawImage>().color = new Color(255, 255, 255 );
            }
        }
    }*/
}
